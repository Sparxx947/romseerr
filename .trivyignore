# Trivy-Ausnahmen / Trivy ignores — jeweils mit Begründung.
# (derzeit keine — der frühere AVD-DS-0002 „root user" ist echt behoben: das Image
#  läuft jetzt als non-root, siehe Dockerfile `USER 1000`.)
